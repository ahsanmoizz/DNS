# Phase 2.4: Subnames, Transfers, & Reverse Resolution

## Objective
Implement subname creation, name transfer flows, and primary reverse name selection using the exact simulator visual paths.

## Context
These operations are currently scattered in the generic manager modal. The simulator has explicit subname management views, a transfer warning flow, and a global setting for primary names.

## Tasks

1. **Subname Management (DLY-031→DLY-033, UX-124, UX-166→UX-167)**
   - Wire the "Create Subname" flow to the simulator's explicit subname tab/modal.
   - Implement the exact subname revoke and resolver selection visuals.

2. **Name Transfer (DLY-021, UX-126)**
   - Wire the Name Transfer flow to the simulator's transfer screen.
   - Implement the selected-domain risk checkbox (UX-126) that is currently missing from the generic modal.

3. **Primary Reverse Name (DLY-022)**
   - Bind the primary reverse name setting to the simulator's exact placement.

## Verification
- Create, manage, and revoke a subname via the simulator UI.
- Transfer a name using the risk-checkbox flow.
- Set a primary reverse name and verify it displays correctly in the profile.
